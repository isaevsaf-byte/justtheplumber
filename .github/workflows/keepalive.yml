name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 */6 * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -f ${{ secrets.HEALTH_URL }} --max-time 30 || echo "Failed"
